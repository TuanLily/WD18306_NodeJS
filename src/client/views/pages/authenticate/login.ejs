<style>
  @keyframes gradientAnimation {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  .section-background {
    background: linear-gradient(45deg, #b0b434, rgb(97, 255, 113));
    background-size: 200% 200%;
    animation: gradientAnimation 10s ease infinite;
  }

  .return-link {
    color: #333; /* Màu của liên kết */
    text-decoration: none; /* Xóa gạch chân của liên kết */
    transition: color 0.3s; /* Hiệu ứng chuyển đổi màu trong 0.3 giây */
  }
  .return-link:hover {
    color: rgb(236, 241, 86); /* Màu của liên kết khi hover */
  }
  .return-link i {
    margin-right: 5px; /* Khoảng cách giữa icon và văn bản */
  }
</style>

<section class="vh-100 section-background">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col col-xl-10">
        <!-- Thêm liên kết trở về vào đây -->
        <a href="/" class="return-link">
          <i class="fas fa-arrow-left"></i> Trở về
        </a>
        <!-- Kết thúc liên kết trở về -->
        <div class="card" style="border-radius: 1rem">
          <div class="row g-0">
            <div class="col-md-6 col-lg-5 d-none d-md-block">
              <img
                src="../public/images/categories_img_02.jpg"
                alt="login form"
                class="img-fluid"
                style="border-radius: 1rem 0 0 1rem; height: 80%"
              />
            </div>
            <div class="col-md-6 col-lg-7 d-flex align-items-center">
              <div class="card-body p-4 p-lg-5 text-black">
                <form id="loginForm" action="/login" method="post">
                  <div class="d-flex align-items-center mb-3 pb-1">
                    <img
                      src="../public/images/favicon.ico"
                      alt="login form"
                      class="img-fluid"
                    />
                    <span class="h1 fw-bold mb-0">Freshshop</span>
                  </div>

                  <h5 class="fw-normal mb-3 pb-3" style="letter-spacing: 1px">
                    Đăng nhập tài khoản
                  </h5>

                  <div class="form-outline mb-4">
                    <label class="form-label" for="emailInput"
                      >Email address</label
                    >
                    <input
                      type="email"
                      id="emailInput"
                      name="email"
                      class="form-control form-control-lg"
                    />
                    <div class="emailError"></div>
                  </div>

                  <div class="form-outline mb-4">
                    <label class="form-label" for="passwordInput"
                      >Password</label
                    >
                    <input
                      type="password"
                      id="passwordInput"
                      name="password"
                      class="form-control form-control-lg"
                    />
                    <div class="passwordError"></div>
                  </div>

                  <div class="pt-1 mb-4">
                    <button
                      class="btn btn-dark btn-lg btn-block"
                      id="loginButton"
                      type="submit"
                    >
                      Login
                    </button>
                  </div>

                  <p class="mb-5 pb-lg-2" style="color: #393f81">
                    Bạn chưa có tài khoản?
                    <a href="/register" style="color: #393f81"
                      >Đăng ký ngay ở đây</a
                    >
                  </p>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- <script>
  document
    .getElementById("loginForm")
    .addEventListener("submit", function (event) {
      event.preventDefault(); // Ngăn chặn hành động mặc định của form

      const email = document.getElementById("emailInput").value.trim();
      const password = document.getElementById("passwordInput").value.trim();

      const emailError = document.querySelector(".emailError");
      const passwordError = document.querySelector(".passwordError");

      function validateEmail(email) {
        const re = /\S+@\S+\.\S+/;
        return re.test(email);
      }

      let hasError = false;

      // Kiểm tra email không được trống và đúng định dạng
      if (!email) {
        emailError.innerHTML = `<span class="text-danger">Email không được để trống</span>`;
        hasError = true;
      } else if (!validateEmail(email)) {
        emailError.innerHTML = `<span class="text-danger">Email không đúng định dạng</span>`;
        hasError = true;
      } else {
        emailError.innerHTML = "";
      }

      // Kiểm tra password không được trống
      if (!password) {
        passwordError.innerHTML = `<span class="text-danger">Mật khẩu không được để trống</span>`;
      } else {
        passwordError.innerHTML = "";
      }

      if (!hasError) {
        // Gửi dữ liệu đăng nhập lên máy chủ (sử dụng fetch hoặc AJAX)
        fetch("/login", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
          },
          body: JSON.stringify({
            email: email,
            password: password,
          }),
        })
          .then((response) => {
            if (!response.ok) {
              Swal.fire({
                icon: "error",
                title: "Đăng nhập không thành công",
                text: "Tài khoản hoặc mật khẩu không đúng.",
              });
              return; // Kết thúc xử lý ở đây nếu response không thành công
            }
            return response.json(); // Chuyển sang phần xử lý thành công nếu response thành công
          })
          .then((data) => {
            if (!data) return; // Kết thúc xử lý ở đây nếu không có dữ liệu trả về
            Swal.fire({
              icon: "success",
              title: "Đăng nhập thành công",
              text: "Bạn sẽ được chuyển hướng trong giây lát.",
              showConfirmButton: false,
              timer: 1500, // Thời gian hiển thị thông báo
            }).then(() => {
              // Kiểm tra phản hồi từ máy chủ và chuyển hướng đến trang chính nếu đăng nhập thành công
              if (data.redirectUrl) {
                window.location.href = data.redirectUrl;
              }
            });
          })
          .catch((error) => {
            console.error(
              "There was a problem with your fetch operation:",
              error
            );
          });
      }
    });
</script> -->
